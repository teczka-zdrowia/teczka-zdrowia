mutation($id: ID!, $data: UpdateAppointmentInput!) {
  updateAppointment(id: $id, data: $data) {
    id
    date
    note
    confirmed
    patient {
      id
      avatar
      name
      phone
    }
    place {
      id
      name
    }
  }
}
